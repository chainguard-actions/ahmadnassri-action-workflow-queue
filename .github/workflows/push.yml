# ---------------------------------------------------- #
# Note: this file originates in template-action-docker #
# ---------------------------------------------------- #

name: push

on:
  - push
  - workflow_dispatch

permissions:
  contents: read

jobs:
  main:
    uses: ahmadnassri/actions/.github/workflows/push-action-docker.yml@d4fbab18738500fb2cdf8e23ab3fb79374fcb375 # master
    secrets: inherit
    permissions:
      contents: write
      statuses: write
      packages: write
      pull-requests: write
